# Session configuration
SESSION_SECRET=your-session-secret-change-this

# Database configuration for local development
DATABASE_URL=postgresql://username:password@localhost:5432/syncboard

# Set to development for local environment
NODE_ENV=development

# These are dummy values for local development, they aren't used since we mock Supabase
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=local-development-key